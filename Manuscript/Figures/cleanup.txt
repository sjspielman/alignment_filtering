#fubar <- read.table('data/short_fubar_sweeps_rho.txt', header=T)

fubar2<-data.frame()
for (i in 1:nrow(fubar))
{
	if (fubar[i,]$cutoff %in% cuts)
	{
		fubar2<-rbind(fubar, fubar[i,])
	}
}


paml  <- read.table('data/short_paml_sweeps_rho.txt', header=T)
paml2<-data.frame()
for (i in 1:nrow(paml))
{
	print(i)
	if (paml[i,]$cutoff %in% cuts)
	{
		paml2<-rbind(paml, paml[i,])
	}
}

